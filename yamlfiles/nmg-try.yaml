apiVersion: apps/v1
kind: Deployment
metadata: {name: nmg-try}
spec:
  replicas: 1
  selector:
    matchLabels: {app: nmg-try}
  template:
    metadata:
      labels: {app: nmg-try}
    spec:
      containers:
      - image: ashspencil/k8s-ml:9.0-cudnn7-devel-ssh
        name: nmg-try
        ports:
        - {containerPort: 22, name: ssh}
        resources:
          limits: {memory: 50Gi, nvidia.com/gpu: 1}
          requests: {memory: 25.0Gi}
      nodeSelector: {accelerator: nvidia-tesla-k20, accelerator2: nvidia-tesla-k20x}
