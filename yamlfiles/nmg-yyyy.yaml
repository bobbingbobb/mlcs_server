apiVersion: apps/v1
kind: Deployment
metadata: {name: nmg-yyyy}
spec:
  replicas: 1
  selector:
    matchLabels: {app: nmg-yyyy}
  template:
    metadata:
      labels: {app: nmg-yyyy}
    spec:
      containers:
      - image: ashspencil/k8s-ml:9.0-cudnn7-devel-ssh
        name: nmg-yyyy
        ports:
        - {containerPort: 22, name: ssh}
        resources:
          limits: {memory: 8Gi, nvidia.com/gpu: 1}
          requests: {memory: 4.0Gi}
      nodeSelector: {accelerator3: nvidia-quadro-k620}
