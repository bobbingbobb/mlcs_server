apiVersion: apps/v1
kind: Deployment
metadata: {name: nmg-f}
spec:
  replicas: 1
  selector:
    matchLabels: {app: nmg-f}
  template:
    metadata:
      labels: {app: nmg-f}
    spec:
      containers:
      - image: ashspencil/k8s-ml:9.0-cudnn7-devel-ssh
        name: nmg-f
        ports:
        - {containerPort: 22, name: ssh}
        resources:
          limits: {memory: 4Gi, nvidia.com/gpu: 0}
          requests: {memory: 2.0Gi}
      nodeSelector: {}
