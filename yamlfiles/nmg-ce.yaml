apiVersion: apps/v1
kind: Deployment
metadata: {name: nmg-ce}
spec:
  replicas: 1
  selector:
    matchLabels: {app: nmg-ce}
  template:
    metadata:
      labels: {app: nmg-ce}
    spec:
      containers:
      - image: ashspencil/centos-ssh:v1
        name: nmg-ce
        ports:
        - {containerPort: 22, name: ssh}
        resources:
          limits: {memory: 5Gi, nvidia.com/gpu: 0}
          requests: {memory: 2.5Gi}
      nodeSelector: {}
