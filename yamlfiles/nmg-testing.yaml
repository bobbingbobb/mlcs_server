apiVersion: apps/v1
kind: Deployment
metadata: {name: nmg-testing}
spec:
  replicas: 1
  selector:
    matchLabels: {app: nmg-testing}
  template:
    metadata:
      labels: {app: nmg-testing}
    spec:
      containers:
      - image: ashspencil/k8s-ml:9.0-cudnn7-devel-ssh
        name: nmg-testing
        ports:
        - {containerPort: 22, name: ssh}
        resources:
          limits: {memory: 50Gi, nvidia.com/gpu: 1}
          requests: {memory: 25.0Gi}
      nodeSelector: {accelerator2: nvidia-tesla-k20x}
