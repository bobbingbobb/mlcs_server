apiVersion: apps/v1
kind: Deployment
metadata: {name: nmg-quadrok20}
spec:
  replicas: 1
  selector:
    matchLabels: {app: nmg-quadrok20}
  template:
    metadata:
      labels: {app: nmg-quadrok20}
    spec:
      containers:
      - image: ashspencil/k8s-ml:9.0-cudnn7-devel-ssh
        name: nmg-quadrok20
        ports:
        - {containerPort: 22, name: ssh}
        resources:
          limits: {memory: 8Gi, nvidia.com/gpu: 1}
          requests: {memory: 4.0Gi}
      nodeSelector: {accelerator3: nvidia-quadro-k620}
