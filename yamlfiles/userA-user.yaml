apiVersion: apps/v1
kind: Deployment
metadata: {name: userA-user}
spec:
  replicas: 1
  selector:
    matchLabels: {app: userA-user}
  template:
    metadata:
      labels: {app: userA-user}
    spec:
      containers:
      - image: ashspencil/k8s-ml:9.0-cudnn7-devel-ssh
        name: userA-user
        ports:
        - {containerPort: 22, name: ssh}
        resources:
          limits: {memory: 20Gi, nvidia.com/gpu: 1}
          requests: {memory: 10.0Gi}
      nodeSelector: {accelerator3: nvidia-quadro-k620}
