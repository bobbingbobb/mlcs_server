apiVersion: apps/v1
kind: Deployment
metadata: {name: nmg-s}
spec:
  replicas: 1
  selector:
    matchLabels: {app: nmg-s}
  template:
    metadata:
      labels: {app: nmg-s}
    spec:
      containers:
      - image: ashspencil/eg_sshd:v1
        name: nmg-s
        ports:
        - {containerPort: 22, name: ssh}
        resources:
          limits: {memory: 5Gi, nvidia.com/gpu: 0}
          requests: {memory: 2.5Gi}
      nodeSelector: {}
