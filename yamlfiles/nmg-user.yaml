apiVersion: apps/v1
kind: Deployment
metadata: {name: nmg-user}
spec:
  replicas: 1
  selector:
    matchLabels: {app: nmg-user}
  template:
    metadata:
      labels: {app: nmg-user}
    spec:
      containers:
      - image: ashspencil/k8s-ml:9.0-cudnn7-devel-ssh
        name: nmg-user
        ports:
        - {containerPort: 22, name: ssh}
        resources:
          limits: {memory: 20Gi, nvidia.com/gpu: 1}
          requests: {memory: 10.0Gi}
      nodeSelector: {accelerator3: nvidia-quadro-k620}
